# Deploy 目录文件分类说明

> **最后更新**: 已清理完成，删除了所有一次性修复脚本

## ✅ 当前保留的核心文件

### 服务配置（必须保留）
- `my-fullstack-app.service` - systemd 服务配置（HTTP）
- `my-fullstack-app-ssl.service` - systemd 服务配置（HTTPS）
- `nginx.conf` - Nginx 配置（HTTP）
- `nginx-ssl.conf` - Nginx 配置（HTTPS）

### 同步脚本（主要使用）
- `sync-on-server-complete.sh` - 服务器端完整同步脚本（主要使用，包含所有修复步骤）
- `sync-quick.ps1` - Windows 快速同步脚本（主要使用）
- `sync-to-server-robust.ps1` - 备用同步脚本

### SSL/HTTPS 相关（使用 HTTPS）
- `upload-ssl-cert.bat` - SSL 证书上传（Windows）
- `upload-ssl-cert.ps1` - SSL 证书上传（PowerShell）

### Docker/Milvus 相关（使用 Docker）
- `install-docker-aliyun.sh` - Docker 安装脚本（阿里云）
- `install-docker-aliyun-low-memory.sh` - Docker 安装脚本（低内存）
- `install-docker-compose.sh` - Docker Compose 安装脚本
- `milvus-docker-compose.yml` - Milvus Docker Compose 配置
- `check-milvus-safe.sh` - Milvus 安全检查脚本
- `check-milvus-status.sh` - Milvus 状态检查脚本
- `configure-milvus-firewall.sh` - Milvus 防火墙配置
- `ensure-docker-running.sh` - 确保 Docker 运行

### 其他工具脚本
- `start-service-complete.sh` - 服务启动脚本
- `configure-firewall.sh` - 防火墙配置

### 文档
- `README.md` - 部署说明文档
- `README_SYNC.md` - 同步说明文档
- `文件分类说明.md` - 本文件

## 📊 清理说明

已删除的文件类别：
- ✅ 一次性修复脚本（Attu、数据库权限、Nginx配置、封面图片等）
- ✅ 重复的 Milvus 启动脚本（保留核心配置）
- ✅ 重复的 Docker 镜像拉取脚本（保留安装脚本）
- ✅ 过时的同步脚本（保留主要使用的脚本）
- ✅ 临时测试脚本（backend/test_cover_images.py）
- ✅ 过时的文档文件（FINAL_SOLUTION.md, QUICK_FIX_NETWORK.md, MILVUS_README.md）

**注意**：
- 所有一次性修复脚本已删除，修复逻辑已集成到 `sync-on-server-complete.sh`
- 系统现在完全使用数据库存储数据
- 封面图片路径修复、数据库权限修复等功能已集成到主同步脚本中

## 📝 使用说明

### 主要使用的脚本

1. **代码同步**：
   - Windows: `.\scripts\deploy\sync-quick.ps1`
   - 服务器: `bash /var/www/my-fullstack-app/scripts/deploy/sync-on-server-complete.sh`

2. **服务配置**：
   - HTTP: `my-fullstack-app.service` + `nginx.conf`
   - HTTPS: `my-fullstack-app-ssl.service` + `nginx-ssl.conf`

3. **SSL 配置**：
   - `apply-ssl-complete-fixed.sh` - 应用 SSL 配置（如果存在）
   - `upload-ssl-cert.ps1` - 上传证书

4. **Docker 安装**：
   - `install-docker-aliyun.sh` - 标准安装
   - `install-docker-aliyun-low-memory.sh` - 低内存环境

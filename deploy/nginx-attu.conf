# Nginx 配置 - Attu 管理界面反向代理
# 将此文件复制到 /etc/nginx/conf.d/attu.conf

server {
    listen 80;
    server_name attu.linchuan.tech;  # 替换为你的域名或使用 IP

    location / {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket 支持
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# HTTPS 配置（如果配置了 SSL）
# server {
#     listen 443 ssl http2;
#     server_name attu.linchuan.tech;
# 
#     ssl_certificate /etc/nginx/ssl/linchuan.tech/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/linchuan.tech/privkey.pem;
# 
#     location / {
#         proxy_pass http://localhost:3000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
# }


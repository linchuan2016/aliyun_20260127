# 数据库配置
# SQLite（默认，用于开发）
DATABASE_URL=sqlite:////app/data/products.db

# MySQL（生产环境可选）
# DATABASE_URL=mysql+pymysql://user:password@mysql:3306/myapp

# 后端服务配置
HOST=0.0.0.0
PORT=8000

# CORS 允许的来源（逗号分隔）
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173,http://localhost:80

# JWT 密钥（生产环境必须修改）
# SECRET_KEY=your-secret-key-here
# ALGORITHM=HS256
# ACCESS_TOKEN_EXPIRE_MINUTES=30

